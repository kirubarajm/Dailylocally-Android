package com.dailylocally.utilities;


import android.view.LayoutInflater;
import android.view.View;
import android.widget.TextView;

import com.dailylocally.R;
import com.google.android.gms.maps.GoogleMap;
import com.google.android.gms.maps.model.Marker;

public class CustomMarkerInfoWindowView /*implements GoogleMap.InfoWindowAdapter*/ {

     /*   private final View markerItemView;
    String title;
    String content;

        public CustomMarkerInfoWindowView(String title,String content) {
            this.title=title;
            this.content=content;
            markerItemView = la.inflate(R.layout.marker_info_window, null);  // 1
        }

        @Override
        public View getInfoWindow(Marker marker) { // 2
            TextView itemNameTextView = markerItemView.findViewById(R.id.title);
            TextView itemAddressTextView = markerItemView.findViewById(R.id.content);
            itemNameTextView.setText(title);
            itemAddressTextView.setText(content);
            return markerItemView;  // 4
        }
        @Override
        public View getInfoContents(Marker marker) {
            return null;
        }*/
    }